def exchangeList(list):
    temp=0

    for i in range(0, (len(list) // 2) ):
        temp = list[i]
        list[i] = list[i]

def main():
    list = [4, 66, 33, 5, 7, 1]
    # exchangeList(list)





main()